#!/bin/bash

VER=@MEMEVER@
DEPLOYDIR=/opt/opal/deployed


echo undeploying FIMO_${VER}
rm $DEPLOYDIR/FIMO_${VER}.xml

echo undeploying MEME_${VER}
rm $DEPLOYDIR/MEME_${VER}.xml

echo undeploying MAST_${VER}
rm $DEPLOYDIR/MAST_${VER}.xml

echo undeploying GLAM2_${VER}
rm $DEPLOYDIR/GLAM2_${VER}.xml

echo undeploying GLAM2SCAN_${VER}
rm $DEPLOYDIR/GLAM2SCAN_${VER}.xml

echo undeploying GOMO_${VER}
rm $DEPLOYDIR/GOMO_${VER}.xml

echo undeploying MCAST_${VER}
rm $DEPLOYDIR/MCAST_${VER}.xml

echo undeploying TOMTOM_${VER}
rm $DEPLOYDIR/TOMTOM_${VER}.xml

echo undeploying SPAMO_${VER}
rm $DEPLOYDIR/SPAMO_${VER}.xml

echo undeploying MEMECHIP_${VER}
rm $DEPLOYDIR/MEMECHIP_${VER}.xml

echo undeploying DREME_${VER}
rm $DEPLOYDIR/DREME_${VER}.xml

echo undeploying CENTRIMO_${VER}
rm $DEPLOYDIR/CENTRIMO_${VER}.xml

