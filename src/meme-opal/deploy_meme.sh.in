#!/bin/bash

VER=@MEMEVER@
OPALCONFIG=/opt/meme_@MEMEVER@/etc
DEPLOYDIR=/opt/opal/deployed

echo deploy FIMO_${VER}
cp $OPALCONFIG/fimo_config.xml $DEPLOYDIR/FIMO_${VER}.xml

echo deploy MEME_${VER}
cp $OPALCONFIG/meme_config.xml $DEPLOYDIR/MEME_${VER}.xml

echo deploy MAST_${VER}
cp $OPALCONFIG/mast_config.xml $DEPLOYDIR/MAST_${VER}.xml

echo deploy GLAM2_${VER}
cp $OPALCONFIG/glam2_config.xml $DEPLOYDIR/GLAM2_${VER}.xml

echo deploy GLAM2SCAN_${VER}
cp $OPALCONFIG/glam2scan_config.xml $DEPLOYDIR/GLAM2SCAN_${VER}.xml

echo deploy GOMO_${VER}
cp $OPALCONFIG/gomo_config.xml $DEPLOYDIR/GOMO_${VER}.xml

echo deploy MCAST_${VER}
cp $OPALCONFIG/mcast_config.xml $DEPLOYDIR/MCAST_${VER}.xml

echo deploy TOMTOM_${VER}
cp $OPALCONFIG/tomtom_config.xml $DEPLOYDIR/TOMTOM_${VER}.xml

echo deploy SPAMO_${VER}
cp $OPALCONFIG/spamo_config.xml $DEPLOYDIR/SPAMO_${VER}.xml

echo deploy MEMECHIP_${VER}
cp $OPALCONFIG/meme-chip_config.xml $DEPLOYDIR/MEMECHIP_${VER}.xml

echo deploy DREME_${VER}
cp $OPALCONFIG/dreme_config.xml $DEPLOYDIR/DREME_${VER}.xml

echo deploy CENTRIMO_${VER}
cp $OPALCONFIG/centrimo_config.xml $DEPLOYDIR/CENTRIMO_${VER}.xml

